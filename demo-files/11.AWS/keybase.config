curl --remote-name https://prerelease.keybase.io/keybase_amd64.deb
sudo apt install -y ./keybase_amd64.deb

export KEYBASE_PAPERKEY=${KEYBASE_PAPERKEY}
export KEYBASE_DEVICENAME=${labuser}
keybase login <keybase_user>
